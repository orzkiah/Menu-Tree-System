# Screenshots

Drop the following PNGs here (referenced by the root `README.md`):

| File | What to capture |
| ---- | --------------- |
| `empty-state.png` | Tree empty state with the **Create Menu** CTA |
| `create-root.png` | Add Menu modal creating a root menu |
| `create-child.png` | Adding a child via the row **+** action (parent pre-selected) |
| `edit-menu.png` | Edit modal pre-filled with an existing menu |
| `delete-confirmation.png` | Delete confirmation dialog |
| `final-tree.png` | Fully populated tree with connector lines |

> Tip: run the app (`docker compose up` or local dev), then use your OS screenshot
> tool. Keep widths ~1280px for desktop shots.
